<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <% d = DateTime.now %>
    <% date = "#{d.strftime("%b %d, %Y")}" %>
    <h1>Today's date is <%=date%></h1>
  </body>
</html>
